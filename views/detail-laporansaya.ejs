<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Detail Laporan Saya</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-gray-100 text-gray-800">
    <!-- Main Content -->
    <main class="min-h-screen flex items-center justify-center p-6">
      <div
        class="bg-white rounded-xl shadow-lg p-8 w-full max-w-6xl mx-auto my-8"
      >
        <h3 class="text-2xl font-bold text-center text-gray-800 mb-6">
          Detail Laporan
        </h3>

        <% if (report) { %>
        <!-- Nama Barang Section -->
        <div class="mb-6">
          <p class="text-2xl font-semibold text-center text-gray-900">
            <%= report.nama_barang || '-' %>
          </p>
        </div>

        <!-- Foto Barang Section -->
        <% if (report.foto_barang) { %>
        <div class="mb-6 flex justify-center">
          <div class="w-64 h-64 bg-gray-200 rounded-lg overflow-hidden">
            <img
              src="/<%= report.foto_barang %>"
              alt="Foto Barang"
              class="object-cover w-full h-full"
            />
          </div>
        </div>
        <% } else { %>
        <p><strong>Foto Barang:</strong> -</p>
        <% } %>

        <!-- Jenis Laporan Section -->
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Jenis Laporan:</p>
          <p><%= report.jenis_laporan || '-' %></p>
        </div>

        <!-- Tanggal Kejadian Section -->
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Tanggal Kejadian:</p>
          <p>
            <%= new Date(report.tanggal_kejadian).toLocaleDateString('id-ID', {
            weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' })
            || '-' %>
          </p>
        </div>

        <!-- Lokasi Section -->
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Lokasi:</p>
          <p><%= report.lokasi || '-' %></p>
        </div>

        <!-- Tanggal Laporan Section -->
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Tanggal Laporan:</p>
          <p>
            <%= new Date(report.tanggal_laporan).toLocaleDateString('id-ID', {
            weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' })
            || '-' %>
          </p>
        </div>

        <!-- Deskripsi Section -->
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Deskripsi:</p>
          <p><%= report.deskripsi || '-' %></p>
        </div>

        <!-- Status Section -->
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Status:</p>
          <p><%= report.status || '-' %></p>
        </div>

        <!-- Penemu/Penerima Section -->
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Nama Penemu/Penerima:</p>
          <p><%= report.nama_penemu_penerima || '-' %></p>
        </div>

        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">No HP Penemu/Penerima:</p>
          <p><%= report.nohp_penemu_penerima || '-' %></p>
        </div>

        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Tanggal Penyerahan:</p>
          <p><%= report.tanggal_penyerahan || '-' %></p>
        </div>

        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Lokasi Penyerahan:</p>
          <p><%= report.lokasi_penyerahan || '-' %></p>
        </div>

        <!-- Foto Bukti Penyerahan Section -->
        <% if (report.foto_bukti) { %>
        <div class="mb-6">
          <p class="font-bold text-lg text-gray-700">Foto Bukti Penyerahan:</p>
          <div class="w-64 h-64 bg-gray-200 rounded-lg overflow-hidden">
            <img
              src="/<%= report.foto_bukti %>"
              alt="Foto Bukti Penyerahan"
              class="object-cover w-full h-full"
            />
          </div>
        </div>
        <% } else { %>
        <p><strong>Foto Bukti Penyerahan:</strong> -</p>
        <% } %>

        <!-- Back Button -->
        <div class="mt-8 text-center">
          <a
            href="/my-report"
            class="inline-block bg-green-600 text-white py-2 px-6 rounded-lg hover:bg-green-700 transition duration-300"
            >Kembali ke Daftar Laporan</a
          >
        </div>

        <% } else { %>
        <p class="text-center text-gray-600">Laporan tidak ditemukan.</p>
        <% } %>
      </div>
    </main>
  </body>
</html>
