<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Kontak Pelapor</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 font-sans leading-relaxed tracking-wide">

  <div class="max-w-xl mx-auto mt-10 p-6 bg-white rounded-xl shadow-md">
    <h1 class="text-2xl font-bold text-green-600 mb-6">Kontak Pelapor</h1>

    <div class="space-y-3 text-gray-800">
      <p><span class="font-semibold">Nama Pelapor:</span> <%= laporan.nama %></p>
      <p><span class="font-semibold">Email:</span> <%= laporan.email %></p>
      <p><span class="font-semibold">No. Telepon:</span> <%= laporan.no_telepon %></p>
      <p><span class="font-semibold">Alamat:</span> <%= laporan.alamat %></p>
    </div>

    <hr class="my-6">

    <h2 class="text-xl font-semibold text-gray-700 mb-2">Detail Laporan</h2>
    <div class="space-y-2 text-gray-700">
      <p><span class="font-semibold">Jenis Laporan:</span> <%= laporan.jenis_laporan %></p>
      <p><span class="font-semibold">Nama Barang:</span> <%= laporan.nama_barang %></p>
      <p><span class="font-semibold">Tanggal Kejadian:</span> <%= laporan.tanggal_kejadian.toLocaleDateString('id-ID') %></p>
      <p><span class="font-semibold">Lokasi:</span> <%= laporan.lokasi %></p>
      <p><span class="font-semibold">Deskripsi:</span> <%= laporan.deskripsi %></p>
    </div>

    <% if (laporan.foto_barang) { %>
      <div class="mt-6">
        <p class="font-semibold text-gray-700 mb-1">Foto Barang:</p>
        <img src="/uploads/<%= laporan.foto_barang %>" alt="Foto Barang" class="w-full max-h-80 object-contain rounded shadow-md">
      </div>
    <% } %>

    <div class="mt-8 text-center">
      <a href="/dashboard" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded">Kembali ke Dashboard</a>
    </div>
  </div>

</body>
</html>