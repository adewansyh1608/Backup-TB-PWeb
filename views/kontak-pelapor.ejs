<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Kontak Pelapor</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.3/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans antialiased">
    <div class="min-h-screen flex items-center justify-center">
      <div class="w-full max-w-2xl p-8 bg-white shadow-xl rounded-lg">
        <!-- Heading -->
        <div class="text-center mb-8">
          <h1 class="text-4xl font-extrabold text-green-600 mb-2">
            Kontak Pelapor
          </h1>
          <p class="text-lg text-gray-500">
            Berikut adalah informasi kontak dari pelapor laporan yang Anda
            claim.
          </p>
        </div>

        <!-- Reporter Information Card -->
        <div class="bg-white p-8 rounded-lg shadow-md">
          <div class="space-y-4">
            <div>
              <h2 class="text-2xl font-semibold text-green-600">
                Nama Pelapor
              </h2>
              <p class="text-xl text-gray-700"><%= reporter.nama %></p>
            </div>

            <div>
              <h2 class="text-2xl font-semibold text-green-600">Email</h2>
              <p class="text-xl text-gray-700"><%= reporter.email %></p>
            </div>

            <div>
              <h2 class="text-2xl font-semibold text-green-600">
                Nomor Telepon
              </h2>
              <p class="text-xl text-gray-700"><%= reporter.no_telepon %></p>
            </div>

            <div>
              <h2 class="text-2xl font-semibold text-green-600">Alamat</h2>
              <p class="text-xl text-gray-700"><%= reporter.alamat %></p>
            </div>
          </div>
        </div>

        <!-- Back to Dashboard Button -->
        <div class="mt-6 text-center">
          <a
            href="/dashboard"
            class="inline-block px-8 py-3 text-white bg-green-600 rounded-full font-semibold text-lg hover:bg-green-700 transition duration-300"
          >
            Kembali ke Dashboard
          </a>
        </div>
      </div>
    </div>
  </body>
</html>
